"use client"

// Heavy particle animations removed to reduce CPU/GPU usage and hydration.
export default function AnimatedParticles() {
  // No-op placeholder — renders nothing to avoid expensive canvas work.
  return null
}
