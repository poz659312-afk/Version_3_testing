// Placeholder component for Iridescence
// This is a temporary fix for build issues
interface IridescenceProps {
  color?: [number, number, number];
  mouseReact?: boolean;
  amplitude?: number;
  speed?: number;
}

export default function Iridescence({ color, mouseReact, amplitude, speed }: IridescenceProps) {
  return null;
}
